[project]
name = "nexora-brain"
version = "2.0.0"
description = "Document intelligence and knowledge infrastructure engine built with FastAPI and SQLAlchemy."
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
